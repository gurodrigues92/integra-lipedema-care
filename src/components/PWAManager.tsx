// PWAManager placeholder component to fix runtime error
import React from 'react';

const PWAManager: React.FC = () => {
  return null; // Empty component, PWA functionality disabled
};

export default PWAManager;